import React from 'react'

const Buttons = ({text}) => {
  return (
	 <div>
		<button className='text-[#FAFAFA] bg-[#DB4444] py-[10px] px-[60px] rounded-[5px]'>{text}</button>
	 </div>
  )
}

export default Buttons
